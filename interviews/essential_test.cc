#include "essential.hh"

#include "../utils/test_utils.hh"

/** 
 * Usage: 
 * > g++ essential_test.cc -o essential -std=c++20 -O3
 * > ./essential > out.txt
 * 
 */ 

using milli = std::chrono::duration<double, std::milli>;
using hr_clock = std::chrono::high_resolution_clock;

int main() {
    return 0;
}
